name: Build Go compiler
on: [push]

jobs:
  build:
    runs-on: ubuntu-18.04
    strategy:
      fail-fast: false
    steps:
    - uses: actions/checkout@v1
    - name: Compile Go with make.bash
      run: |
        cd src ; ./make.bash
